import React from 'react';
import { View, Text } from 'react-native';
import styles from '../../../styles/AdminSectionStyles';

const DataSection = ({ datos }) => {
  return (
    <View style={styles.section}>
        <Text>
            Hola Mundo
        </Text>
    </View>
  );
};

export default DataSection;
